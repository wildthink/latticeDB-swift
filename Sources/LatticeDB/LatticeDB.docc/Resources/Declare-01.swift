// Name the labels, edge types, and properties you model once.
extension NodeType {
  static let person: NodeType = "Person"
  static let place: NodeType = "Place"
}

extension EdgeType {
  static let frequents: EdgeType = "FREQUENTS"
}
